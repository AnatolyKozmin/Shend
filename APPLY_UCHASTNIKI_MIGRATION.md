# 🚀 Инструкция по применению миграции для таблицы uchastniki

## 📋 Что нужно сделать

Применить миграцию `003_create_uchastniki_table.py` для создания таблицы участников.

## 🔧 Пошаговая инструкция

### Шаг 1: Проверить, что контейнеры запущены

```bash
# Перейти в директорию проекта
cd /Users/katusha/Desktop/Shend-1

# Проверить статус контейнеров
docker-compose ps

# Если контейнеры не запущены, запустить их
docker-compose up -d
```

### Шаг 2: Войти в контейнер с ботом

```bash
# Узнать имя контейнера
docker-compose ps

# Войти в контейнер (имя может быть shend-1-bot-1 или похожее)
docker exec -it shend-1-bot-1 bash

# Или если используете docker-compose напрямую:
docker-compose exec bot bash
```

### Шаг 3: Проверить текущее состояние миграций

```bash
# Внутри контейнера проверить текущую версию
alembic current

# Посмотреть историю миграций
alembic history
```

### Шаг 4: Применить миграцию

```bash
# Применить все миграции до последней (включая нашу новую)
alembic upgrade head

# Должно появиться сообщение:
# INFO  [alembic.runtime.migration] Running upgrade 002 -> 003, create uchastniki table
```

### Шаг 5: Проверить, что таблица создана

```bash
# Выйти из контейнера бота
exit

# Зайти в контейнер PostgreSQL
docker exec -it shend-1-db-1 psql -U postgres -d shabot_db

# Или через docker-compose:
docker-compose exec db psql -U postgres -d shabot_db
```

Внутри PostgreSQL:

```sql
-- Проверить список таблиц
\dt

-- Должна быть таблица uchastniki

-- Посмотреть структуру таблицы
\d uchastniki

-- Должны быть колонки:
-- id, full_name, course, faculty, telegram_username, tg_id, created_at

-- Выйти из PostgreSQL
\q
```

### Шаг 6: Выйти из контейнера

```bash
# Если вы всё ещё внутри контейнера
exit
```

## 🎯 Быстрый способ (всё в одной команде)

Если контейнеры уже запущены, можно применить миграцию одной командой:

```bash
# Из корня проекта
docker-compose exec bot alembic upgrade head
```

Или если нужно зайти в контейнер:

```bash
docker exec -it $(docker-compose ps -q bot) alembic upgrade head
```

## ⚠️ Возможные проблемы

### Проблема 1: "Container not found" или "No such container"

**Решение:**
```bash
# Проверить имя контейнера
docker ps

# Использовать правильное имя из вывода
docker exec -it <ИМЯ_КОНТЕЙНЕРА> bash
```

### Проблема 2: "alembic: command not found"

**Причина:** Зависимости не установлены или контейнер не пересобран

**Решение:**
```bash
# Пересобрать контейнер
docker-compose build

# Перезапустить
docker-compose up -d

# Попробовать снова
docker-compose exec bot alembic upgrade head
```

### Проблема 3: "Target database is not up to date"

**Причина:** Есть незавершённые миграции

**Решение:**
```bash
# Проверить текущую версию
docker-compose exec bot alembic current

# Посмотреть историю
docker-compose exec bot alembic history

# Применить все миграции
docker-compose exec bot alembic upgrade head
```

### Проблема 4: "Can't locate revision identified by '002'"

**Причина:** Миграция 002 не применена или есть проблемы с порядком

**Решение:**
```bash
# Проверить какие миграции применены
docker-compose exec bot alembic current

# Если нужно, применить миграции по порядку
docker-compose exec bot alembic upgrade 002
docker-compose exec bot alembic upgrade head
```

### Проблема 5: "Table 'uchastniki' already exists"

**Причина:** Таблица уже создана вручную или миграция уже применена

**Решение:**
```bash
# Проверить текущую версию миграций
docker-compose exec bot alembic current

# Если миграция 003 уже применена, всё в порядке
# Если нет, но таблица существует, можно пометить миграцию как применённую:
docker-compose exec bot alembic stamp 003
```

## ✅ Проверка успешного применения

После применения миграции проверьте:

1. **Таблица создана:**
   ```bash
   docker-compose exec db psql -U postgres -d shabot_db -c "\d uchastniki"
   ```

2. **Миграция применена:**
   ```bash
   docker-compose exec bot alembic current
   # Должно показать: 003 (head)
   ```

3. **Можно импортировать данные:**
   ```bash
   # После того как подготовите uchast.xlsx
   docker-compose exec bot python scripts/load_uchastniki.py
   ```

## 📝 Следующие шаги

После успешного применения миграции:

1. ✅ Подготовьте файл `uchast.xlsx` с данными участников
2. ✅ Запустите импорт: `docker-compose exec bot python scripts/load_uchastniki.py`
3. ✅ Проверьте результаты в базе данных

---

**Готово!** Таблица `uchastniki` создана и готова к использованию! 🎉

