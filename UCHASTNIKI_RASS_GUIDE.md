# 📢 Команда /uch_rass - Рассылка участникам

## Описание

Команда `/uch_rass` предназначена для рассылки сообщений всем участникам из таблицы `uchastniki`, у которых есть `tg_id` (то есть они зарегистрированы в боте).

## Использование

### Шаг 1: Запуск команды

```
/uch_rass
```

### Шаг 2: Просмотр статистики

Бот покажет статистику:
- 📋 Всего участников в базе
- ✅ С tg_id (получат рассылку)
- ⚠️ Без tg_id (не получат, так как не в боте)

### Шаг 3: Отправка текста

Отправьте текст сообщения, которое нужно разослать участникам.

### Шаг 4: Ожидание результата

Бот начнёт рассылку и покажет прогресс.

## Пример использования

```
Админ: /uch_rass

Бот: 📢 Рассылка участникам

📊 Статистика:
   📋 Всего участников: 120
   ✅ С tg_id (получат рассылку): 85
   ⚠️  Без tg_id (не получат): 35

Пришлите текст сообщения, которое нужно разослать 85 участникам:

Админ: Привет! Напоминаю о важном событии завтра в 18:00.

Бот: 🔄 Начинаю рассылку 85 участникам...

[рассылка происходит]

Бот: ✅ Рассылка участникам завершена!

==============================
📊 СТАТИСТИКА РАССЫЛКИ
==============================

📋 Всего участников с tg_id: 85

✅ Успешно отправлено: 82

❌ Ошибок всего: 3
   🚫 Заблокировали бота: 2
   👤 Пользователь не найден: 1

──────────────────────────────
📈 Успешность: 96.5%
📉 Не доставлено: 3 (3.5%)
==============================
```

## Статистика рассылки

После завершения рассылки бот показывает:

### Основные показатели:
- **Всего участников с tg_id** - сколько участников могут получить рассылку
- **Успешно отправлено** - сколько сообщений доставлено
- **Ошибок всего** - общее количество ошибок

### Детализация ошибок:
- **🚫 Заблокировали бота** - пользователи заблокировали бота
- **👤 Пользователь не найден** - пользователь удалил аккаунт или не существует
- **⚠️ Другие ошибки** - прочие проблемы (сеть, таймауты и т.д.)

### Процент успешности:
- **📈 Успешность** - процент успешно доставленных сообщений
- **📉 Не доставлено** - количество и процент недоставленных сообщений

## Важные замечания

1. **Только участники с tg_id** - рассылка отправляется только тем участникам, у которых есть `tg_id` (они зарегистрированы в боте)

2. **Пауза между отправками** - между отправками есть пауза 0.1 секунды, чтобы не превысить лимиты Telegram API

3. **Обработка ошибок** - бот автоматически обрабатывает различные типы ошибок и показывает детальную статистику

4. **Только для админа** - команда доступна только администратору (ID: 922109605)

## Что делать, если нет участников с tg_id?

Если бот показывает, что нет участников с `tg_id`:

1. ✅ Убедитесь, что данные импортированы из `uchast.xlsx`:
   ```bash
   docker-compose exec bot python scripts/load_uchastniki.py
   ```

2. ✅ Проверьте, что участники связаны с `BotUser`:
   - У участника должен быть `telegram_username` в таблице `uchastniki`
   - В таблице `BotUser` должен быть пользователь с таким же `telegram_username`
   - Скрипт импорта автоматически связывает участников с `BotUser` по `telegram_username`

3. ✅ Проверьте данные в базе:
   ```sql
   -- Войти в PostgreSQL
   docker-compose exec db psql -U postgres -d shabot_db
   
   -- Проверить участников
   SELECT id, full_name, telegram_username, tg_id FROM uchastniki LIMIT 10;
   
   -- Проверить BotUser
   SELECT id, tg_id, telegram_username FROM bot_users LIMIT 10;
   ```

## Сравнение с другими командами рассылки

| Команда | Получатели | Особенности |
|---------|-----------|-------------|
| `/uch_rass` | Участники из `uchastniki` с `tg_id` | Только участники, автоматическая связь с BotUser |
| `/create_all_rass` | Все пользователи из `BotUser` | Всем зарегистрированным в боте |
| `/create_rass` | По факультету из `Person` | Фильтрация по факультету |
| `/create_reserv_rass` | Из таблицы `Reserv` | Для резервистов |

---

**Готово!** Команда `/uch_rass` готова к использованию! 🚀

