# 🚀 Инструкция по применению миграции для новой системы резерва

## 📋 Что было добавлено

### Новые модели в БД:

1. **ReservTimeSlot** - временные слоты для системы резерва
   - Хранит слоты для листов "резерв" и "финфак"
   - Привязка к собеседующим (Interviewer)
   - Даты и времена слотов

2. **ReservBooking** - записи на собеседования
   - Привязка к слотам, собеседующим и пользователям
   - Статус записи (confirmed/cancelled)

### Новый функционал:

- ✅ Парсер для листов "резерв" и "финфак"
- ✅ Команда `/parse_reserv` с детальным дебаг-выводом
- ✅ Статистика по собеседующим и слотам

---

## 🐳 Применение миграции в Docker

### Вариант 1: Если бот УЖЕ запущен в Docker

```bash
# 1. Переходим в папку проекта
cd /path/to/Shend

# 2. Обновляем код
git add .
git commit -m "Add new reserv system with parser"
git push

# 3. На сервере делаем git pull
cd /root/Shend  # или ваш путь
git pull

# 4. Входим в контейнер бота
docker exec -it shend-bot-1 bash

# 5. Создаем миграцию
alembic revision --autogenerate -m "add reserv time slots and bookings"

# 6. Применяем миграцию
alembic upgrade head

# 7. Выходим из контейнера
exit

# 8. Перезапускаем бот
docker-compose restart
```

### Вариант 2: Если нужно пересобрать контейнер

```bash
cd /root/Shend  # или ваш путь
git pull

# Останавливаем контейнеры
docker-compose down

# Пересобираем образ
docker-compose build

# Запускаем контейнеры
docker-compose up -d

# Входим в контейнер для миграции
docker exec -it shend-bot-1 bash
alembic revision --autogenerate -m "add reserv time slots and bookings"
alembic upgrade head
exit

# Проверяем логи
docker-compose logs -f bot
```

---

## 💻 Применение миграции локально (БЕЗ Docker)

### 1. Создание виртуального окружения (если еще нет)

```bash
cd /Users/katusha/Desktop/Shend

# Создаем виртуальное окружение
python3 -m venv venv

# Активируем
source venv/bin/activate

# Устанавливаем зависимости
pip install -r requirements.txt
```

### 2. Настройка подключения к PostgreSQL

Создайте или обновите файл `.env`:

```bash
# Database
DATABASE_URL=postgresql://katusha:katusha123@localhost:5432/shend

# Telegram Bot
TOKEN=ваш_токен_бота
```

### 3. Проверка что PostgreSQL запущен

```bash
# Проверяем статус
brew services list | grep postgresql

# Если не запущен - запускаем
brew services start postgresql@15

# Создаем базу данных (если еще нет)
PGPASSWORD='katusha123' psql -U katusha -d postgres -h localhost -c "CREATE DATABASE shend;"
```

### 4. Создание и применение миграции

```bash
cd /Users/katusha/Desktop/Shend

# Активируем виртуальное окружение
source venv/bin/activate

# Создаем миграцию
alembic revision --autogenerate -m "add reserv time slots and bookings"

# Применяем миграцию
alembic upgrade head
```

### 5. Проверка миграции

```bash
# Подключаемся к БД
PGPASSWORD='katusha123' psql -U katusha -d shend -h localhost

# Проверяем что таблицы созданы
\dt

# Должны увидеть:
# - reserv_time_slots
# - reserv_bookings

# Смотрим структуру таблиц
\d reserv_time_slots
\d reserv_bookings

# Выходим
\q
```

---

## 🧪 Тестирование парсера

### 1. Проверка доступа к Google Sheets

Убедитесь что файл `sha-otbor-476513-9c6d0a1d252c.json` (или `credentials.json`) находится в корне проекта.

### 2. Запуск бота

```bash
cd /Users/katusha/Desktop/Shend
source venv/bin/activate
python main.py
```

### 3. Тестирование команды /parse_reserv

В Telegram боте (от админского аккаунта):

```
/parse_reserv
```

**Ожидаемый вывод:**

```
🔄 Начинаю парсинг листов 'резерв' и 'финфак'...

Это может занять несколько секунд...

✅ Парсинг завершен!

📊 Общая статистика:
• Добавлено новых слотов: 45
• Обновлено существующих: 0
• Пропущено (занято или нет в системе): 8

📋 Всего обработано: 53 слотов из Google Sheets

📊 СТАТИСТИКА ПО СОБЕСЕДУЮЩИМ

👤 Иванов Иван (ID: interviewer_001)
   📦 Всего слотов: 8
   📄 резерв: 5 слотов
      ⏰ 09:00, 10:30, 14:15, 15:00, 16:30
   📄 финфак: 3 слота
      ⏰ 12:00, 13:30, 18:00
...
```

### 4. Проверка в БД

```bash
PGPASSWORD='katusha123' psql -U katusha -d shend -h localhost

-- Проверяем количество слотов
SELECT sheet_name, COUNT(*) FROM reserv_time_slots GROUP BY sheet_name;

-- Смотрим примеры слотов
SELECT * FROM reserv_time_slots LIMIT 10;

-- Выходим
\q
```

---

## 🔧 Возможные проблемы и решения

### Проблема: "FileNotFoundError: credentials.json"

**Решение:**
```bash
# Убедитесь что файл credentials есть в корне проекта
ls -la sha-otbor-476513-9c6d0a1d252c.json

# Если файла нет - скопируйте его из безопасного места
```

### Проблема: "APIError: Quota exceeded"

**Решение:**
Слишком много запросов к Google API. Парсер уже имеет защиту с задержками, но если ошибка повторяется:
- Подождите 1-2 минуты
- Запустите `/parse_reserv` снова

### Проблема: "Лист 'резерв' не найден"

**Решение:**
- Проверьте что лист называется именно "резерв" (с маленькой буквы)
- Проверьте что у бота есть доступ к таблице
- Поделитесь таблицей с email: `sha-bot-sobes@sha-otbor-476513.iam.gserviceaccount.com`

### Проблема: "Пропущено: все собеседующие"

**Решение:**
Собеседующие должны быть зарегистрированы в системе через `/register_sobes` ДО парсинга. 

Проверьте наличие в БД:
```sql
SELECT * FROM interviewers;
```

Если собеседующих нет - сначала нужно их зарегистрировать.

---

## 📊 Структура таблиц Google Sheets

### Обязательные требования:

1. **Лист должен называться**: "резерв" или "финфак" (точное совпадение!)

2. **Первая строка (заголовок)**: Времена слотов
   - Колонка A: название (игнорируется)
   - Колонки B-S: времена (09:00, 09:45, 10:30, ...)
   - Колонка T: "ID" или другое

3. **Строки 2-25**: Данные собеседующих
   - Колонка A: **Имя Фамилия** собеседующего
   - Колонки B-S: **"могу"** или **"не могу"**
   - Колонка T: **ID собеседующего** (из таблицы Interviewers)

### Пример структуры:

```
|   A          |  B   |  C   |  D   | ... |   T   |
|--------------|------|------|------|-----|-------|
| Имя          | 9:00 | 9:45 |10:30 | ... |  ID   |
| Иванов Иван  | могу |не могу| могу | ... | iv001 |
| Петрова Анна |не могу| могу | могу | ... | pet02 |
```

---

## ✅ Чеклист перед запуском

- [ ] PostgreSQL запущен и доступен
- [ ] База данных `shend` создана
- [ ] Файл `.env` настроен правильно
- [ ] Credentials для Google Sheets на месте
- [ ] Миграция применена (`alembic upgrade head`)
- [ ] Собеседующие зарегистрированы в системе
- [ ] Таблица Google Sheets имеет правильную структуру
- [ ] Боту дан доступ к таблице
- [ ] Листы "резерв" и "финфак" существуют

---

## 🎉 Готово!

После успешного применения миграции и парсинга система готова к использованию!

**Следующие шаги:**
1. ✅ Парсинг работает - проверено через `/parse_reserv`
2. ⏳ Добавить команды для пользователей (`/reserv`, `/finfak`)
3. ⏳ Протестировать запись на слоты
4. ⏳ Добавить уведомления собеседующим

---

**Нужна помощь?** Напишите @yanejettt

