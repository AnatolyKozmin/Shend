# ⚡ Быстрый деплой системы резерва

## 🚀 Команды для деплоя

```bash
# На сервере
cd /root/Shend
git pull

docker exec -it shend-bot-1 bash

# Внутри контейнера:
pip install pytz==2024.1
alembic revision --autogenerate -m "add reserv and finfak tables"
alembic upgrade head
exit

# Перезапуск
docker-compose restart bot
docker-compose logs -f bot
```

## ✅ Чеклист после деплоя

- [ ] Бот запустился (логи: "Бот работает !")
- [ ] Создать листы "резерв_записи" и "финфак_записи" в Google Sheets
- [ ] Дать доступ: sha-bot-sobes@sha-otbor-476513.iam.gserviceaccount.com
- [ ] Запустить `/parse_reserv` - проверить статистику
- [ ] Запустить `/parse_finfak` - проверить статистику
- [ ] Протестировать `/finfak` (от пользователя с факультетом "Финфак")
- [ ] Протестировать `/reserv` (от любого пользователя)
- [ ] Проверить листы "всеобщая", "финфак_записи", "резерв_записи"

## 🗑️ Очистка БД (для тестирования)

```bash
docker exec -it shend-db-1 psql -U postgres -d ваша_база

TRUNCATE TABLE reserv_bookings CASCADE;
TRUNCATE TABLE reserv_time_slots CASCADE;
TRUNCATE TABLE finfak_bookings CASCADE;
TRUNCATE TABLE finfak_time_slots CASCADE;

\q
```

## 📋 Команды

### Админ:
- `/parse_reserv` - парсинг резерва
- `/parse_finfak` - парсинг финфака

### Пользователи:
- `/finfak` - запись (Финфак, 07.11.2025)
- `/reserv` - запись (все, 08.11.2025)

## 🎯 Готово!

Полная документация: `RESERV_SYSTEM_COMPLETE.md`

